{
  "name": "bare-v8-to-istanbul",
  "version": "1.0.2",
  "description": "Wrapper for v8-to-istanbul that works with Bare",
  "license": "Apache-2.0",
  "author": "Holepunch",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "standard"
  },
  "dependencies": {
    "bare-assert": "^1.0.2",
    "bare-fs": "^4.1.2",
    "bare-module": "^6.1.2",
    "bare-path": "^3.0.0",
    "bare-process": "^4.2.0",
    "bare-url": "^2.1.5",
    "bare-utils": "^1.2.0",
    "v8-to-istanbul": "^9.3.0",
    "which-runtime": "^1.2.1"
  },
  "imports": {
    "assert": {
      "bare": "bare-assert",
      "default": "assert"
    },
    "fs": {
      "bare": "bare-fs",
      "default": "fs"
    },
    "module": {
      "bare": "bare-module",
      "default": "module"
    },
    "path": {
      "bare": "bare-path",
      "default": "path"
    },
    "process": {
      "bare": "bare-process",
      "default": "process"
    },
    "url": {
      "bare": "bare-url",
      "default": "url"
    },
    "util": {
      "bare": "bare-utils",
      "default": "util"
    }
  },
  "devDependencies": {
    "standard": "^17.1.2"
  }
}
